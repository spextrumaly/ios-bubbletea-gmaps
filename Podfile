# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'BubbleTeaLocation' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!
  pod 'GoogleMaps'
  pod 'Alamofire'
  

  target 'BubbleTeaLocationTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'BubbleTeaLocationUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
